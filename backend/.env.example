# Backend Environment Configuration
# ===================================

# ---- Network ----
# Server port
PORT=3001

# Bind address
#   127.0.0.1 = local only (default, safe)
#   0.0.0.0   = LAN accessible (use this if accessing from other machines)
HOST=127.0.0.1

# CORS allowed origins
#   *                                    = allow all (simple setup)
#   http://localhost,http://10.0.0.10     = whitelist specific origins
CORS_ORIGINS=*

# ---- Storage ----
# SQLite database path (relative to backend/)
DB_PATH=bot.db

# Documents directory (relative to backend/)
DOCS_DIR=docs
