# Backend Environment Configuration

# Server port
PORT=3001

# Bind address (0.0.0.0 = LAN accessible, 127.0.0.1 = localhost only)
HOST=0.0.0.0

# SQLite database path
DB_PATH=bot.db

# Documents directory
DOCS_DIR=docs

# CORS allowed origins (comma-separated, or * for all)
# Examples:
#   CORS_ORIGINS=*                         (allow all — default)
#   CORS_ORIGINS=http://localhost           (localhost only)
#   CORS_ORIGINS=http://10.0.0.10,http://localhost  (specific IPs)
CORS_ORIGINS=*
