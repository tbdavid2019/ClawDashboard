# Frontend Environment (Vite)
# ===========================
# These are optional overrides. The frontend auto-detects the API URL
# from the browser's current hostname by default.

# Override API URL (e.g., if backend runs on a different host/port)
# VITE_API_URL=http://10.0.0.10:3001

# Override backend port (default: 3001)
# VITE_BACKEND_PORT=3001
